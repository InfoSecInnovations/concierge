services:
  ollama:
    extends:
      file: user_package/docker-compose-ollama.yml
      service: ollama-gpu
  opensearch-node1:
    extends:
      file: user_package/docker-compose-opensearch.yml
      service: opensearch-node1
  opensearch-dashboards:
    extends:
      file: user_package/docker-compose-opensearch.yml
      service: opensearch-dashboards

volumes:
  opensearch-data1:

networks:
  default:
    name: concierge