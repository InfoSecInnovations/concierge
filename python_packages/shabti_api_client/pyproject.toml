[project]
name = "shabti-api-client"
version = "0.1a6"
requires-python = ">=3.12"
dependencies = [
  "httpx",
  "python-keycloak",
  "shabti-types"
]

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"